// ---------------------------------------------------------------------------
// DESIGN SYSTEM — PATTERNS. §3.
//
// A pattern is two or more primitives composed into a shape the brand repeats.
// It still knows nothing about blinds, prices or customers — that is what keeps
// it here rather than in a feature.
// ---------------------------------------------------------------------------

export { SectionHead } from './SectionHead';
export { SectionBand } from './SectionBand';
