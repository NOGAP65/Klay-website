// klay/one-verb-per-concept — §6. `fetch` is the canonical network verb here,
// and `create` the canonical constructor, so these two are synonyms of a
// choice that has already been made.
export function loadBooking() { return null; }
export const makeLine = () => null;
