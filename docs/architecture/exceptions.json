{
  "//": "THE MACHINE-READABLE HALF OF SPECIFICATION.md §12. ADR-023.",
  "//why": "§12 is the exceptions register and the constitution's own list. Tooling cannot read a markdown table reliably, and a hand-maintained second copy is exactly the silent divergence §13 names. So the paths live here, the prose lives in §12, and tools/verify-exceptions.mjs asserts that every E-number in one appears in the other. Adding an exception still requires an ADR; it now also requires a line here.",
  "//scope": "`excludesFromScope: true` means the paths leave the in-scope count that gates warn -> error promotion. An exception that is not about scope (E-05's CSP, E-06's token literals) does not carry it.",
  "exceptions": [
    {
      "id": "E-01",
      "summary": "homography.ts exempt from size and complexity limits",
      "paths": [
        "src/visualiser/homography.ts"
      ],
      "excludesFromScope": false,
      "note": "PERMANENT, not deferred. These files may never unfreeze. \"On unfreeze\" was a word that sounded like a condition — it named no date, no trigger and no owner. A permanent exception is a documented decision; a temporary one that never ends is rot.",
      "permanent": true,
      "reviewTrigger": "The file is modified for any reason, or the visualiser migrates."
    },
    {
      "id": "E-02",
      "summary": "Canvas2DBlindRenderer.tsx exempt from size limits",
      "paths": [
        "src/visualiser/Canvas2DBlindRenderer.tsx"
      ],
      "excludesFromScope": false,
      "permanent": true,
      "reviewTrigger": "The file is modified for any reason, or the visualiser migrates.",
      "note": "PERMANENT, not deferred. These files may never unfreeze. \"On unfreeze\" was a word that sounded like a condition — it named no date, no trigger and no owner. A permanent exception is a documented decision; a temporary one that never ends is rot."
    },
    {
      "id": "E-03",
      "summary": "CornerPinOverlay.tsx exempt from size limits",
      "paths": [
        "src/visualiser/CornerPinOverlay.tsx"
      ],
      "excludesFromScope": false,
      "permanent": true,
      "reviewTrigger": "The file is modified for any reason, or the visualiser migrates.",
      "note": "PERMANENT, not deferred. These files may never unfreeze. \"On unfreeze\" was a word that sounded like a condition — it named no date, no trigger and no owner. A permanent exception is a documented decision; a temporary one that never ends is rot."
    },
    {
      "id": "E-04",
      "summary": "usePhotoUpload.ts exempt from size limits",
      "paths": [
        "src/visualiser/usePhotoUpload.ts"
      ],
      "excludesFromScope": false,
      "permanent": true,
      "reviewTrigger": "The file is modified for any reason, or the visualiser migrates.",
      "note": "PERMANENT, not deferred. These files may never unfreeze. \"On unfreeze\" was a word that sounded like a condition — it named no date, no trigger and no owner. A permanent exception is a documented decision; a temporary one that never ends is rot."
    },
    {
      "id": "E-05",
      "summary": "style-src 'unsafe-inline' in CSP",
      "paths": [],
      "excludesFromScope": false
    },
    {
      "id": "E-06",
      "summary": "design-system/tokens/* exempt from the no-literal-values rule",
      "paths": [
        "src/design-system/tokens/**"
      ],
      "excludesFromScope": false,
      "note": "A rule-level exemption, not a scope exclusion. These files are in scope for every other rule."
    },
    {
      "id": "E-08",
      "summary": "The visualiser is outside this migration and outside every rule in the specification",
      "paths": [
        "src/visualiser/**",
        "src/visualiser-lab/**",
        "src/pages/VisualiserPage.tsx"
      ],
      "excludesFromScope": true,
      "retiresWhen": "V calls it when the wardrobe work pauses. An owner who can be asked, not a milestone that can recede.",
      "note": "VisualizerLabPage.tsx was removed at the asset phase — the route was deleted, closing E-07. src/visualiser-lab/ is untouched and stays.",
      "owner": "V"
    },
    {
      "id": "E-09",
      "summary": "Permanent re-export shim at src/lib/pricing.ts after the module moves to shared-core",
      "paths": [
        "src/lib/pricing.ts"
      ],
      "excludesFromScope": true,
      "retiresWhen": "With E-08 — V calls it when the wardrobe work pauses.",
      "note": "Not yet a shim. The path is excluded from the moment the exception is granted, so the count does not jump when Phase 6 creates it.",
      "owner": "V"
    },
    {
      "id": "E-10",
      "summary": "src/data/products.ts and src/theme.ts stay where they are",
      "paths": [
        "src/data/products.ts",
        "src/theme.ts"
      ],
      "excludesFromScope": true,
      "retiresWhen": "With E-08 — V calls it when the wardrobe work pauses.",
      "owner": "V"
    },
    {
      "id": "E-11",
      "summary": "Permanent re-export shim at src/components/Nav.tsx after Nav moves to app/layouts/",
      "paths": [
        "src/components/Nav.tsx"
      ],
      "excludesFromScope": true,
      "retiresWhen": "With E-08 — V calls it when the wardrobe work pauses.",
      "owner": "V"
    }
  ]
}
